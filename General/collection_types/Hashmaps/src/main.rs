fn main() {
    let mut scores = Hasmap::new;
    score.insert(Strinfg::from("Blue"),10);
    score.insert(Strinfg::from("Yellow"),50);

    let team_name:String = String:from("Blue");
    let score :i32 = score.get(&team_name).
}
